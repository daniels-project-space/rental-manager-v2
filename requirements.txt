# Wave 4.6 — Hygglo UI automation Python deps.
# Pinned to specific releases verified Feb 2026. Bump deliberately.
#
# browser-use 0.7.x ships the OpenAI-compatible ChatOpenAI client (which
# we point at api.x.ai for Grok), supports use_vision toggle, and works
# on top of Playwright 1.49+.
browser-use==0.7.10
playwright==1.50.0
openai==1.61.0
python-dotenv==1.0.1
