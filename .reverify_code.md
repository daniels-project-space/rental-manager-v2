# Phase C reverify — Ported Listings orchestration routes (T4) + PortedListingsPanel widget (T5)

Read-only inspection. Project: `/home/ubuntu/rental-manager-v2`. Date: 2026-06-18.

## ROUTES (T4)

### Check 1 — `src/app/api/port-listings/run/route.ts` — PASS (with one design note)
- **Exists.** Header: "Ported Listings — batch run trigger (Phase 4, Wave 2, ADDITIVE)".
- **(a) Reads the Convex diff:** `run/route.ts:55` `convex.query(api.ported_listings.getConfig, {key:"leo"})` (require gradient) and `:68` `const { missing } = (await convex.query(api.ported_listings.diff, {}))`. Convex client built at `:49-51` (`CONVEX_URL ?? https://hearty-oyster-600.convex.cloud` — matches verified live prod).
- **(b) Calls the VPS port service with Bearer token:** `:99-101` `serviceUrl = secrets.HYGGLO_PORT_SERVICE_URL`, `serviceToken = secrets.HYGGLO_PORT_SERVICE_TOKEN` (fail-closed 503 if unset, `:101-104`). `:108` `fetch(\`${serviceUrl}/port-batch\`, …)`, `:112` `Authorization: Bearer ${serviceToken}`.
  - NOTE: host is NOT hardcoded as `hygglo-port.87.106.233.113.nip.io` — it comes from vault secret `HYGGLO_PORT_SERVICE_URL`. Endpoint path is `/port-batch` (not `/run`).
- **(c) R2 upload — DESIGN NOTE (not done in this route, by design):** the serverless route does NOT call `getS3Client`/`getR2PublicBase` and does NOT upload PNGs. The VPS performs the full-res 262-image batch + R2 writes itself and reports each result back via Convex HTTP `/port-listings/record` → `ported_listings:upsert` (see route header `:9-10`, `convex/http.ts:35,83`). So the `ported-listings/{productId}.png` key + r2-client usage live on the VPS side, not in the Vercel route. `r2-client.ts` exports `getS3Client` (`:90`) and `getR2PublicBase` (`:118`) and are available, just not used here.
- **(d) Writes status/rows back to Convex:** `:87` `convex.mutation(api.ported_listings.upsert, { … status:"pending" })` seeds rows before dispatch; VPS finishes them via the record callback.

### Check 2 — `src/app/api/port-listings/detect-gradient/route.ts` — PASS
- **Exists.** Header: "Ported Listings — gradient detection", returns `{ ok, orientation, swatches, leoSampleCount }`.
- Pulls Leo image URLs: `:56` `convex.query(api.hygglo_products.list, { accountSlug:"leo" })`, samples ~12 (`LEO_SAMPLE_TARGET=12`), `no_leo_images` 400 if empty (`:66`).
- Calls VPS `/detect-gradient`: `:82-83` `fetch(\`${serviceUrl}/detect-gradient\`)`, Bearer auth `:88`, 503 if env unset.
- Persists profile + returns swatches: `:108` `swatches = Array.isArray(detected.swatches)…`, `:111-115` `convex.mutation(api.ported_listings.setConfig, { gradientProfile:detected, swatches, orientation })`, returns swatches `:120-123`.

### Check 3 — TRIGGER-FREE + no inline batch — PASS
- `grep -rn 'trigger|@trigger.dev' src/app/api/port-listings/` matched ONLY the literal word "trigger" inside a doc comment (`run/route.ts:2` "batch run **trigger**"). NO `@trigger.dev` import in either route or imported helpers (`hygglo-auth`, `convex/browser`, `next/server`).
- **Timeout avoidance:** route does NOT loop the 262 images inline. It seeds `pending` rows then fires ONE async `fetch` to the VPS `/port-batch` and returns immediately (`{ started, count }`). The VPS self-orchestrates the heavy batch and calls back per-image via Convex HTTP `/port-listings/record`. No `maxDuration` override needed — the route is a short kick-off + immediate JSON response, well under Vercel 60s. detect-gradient only samples ~12 images via one VPS round-trip. **No route blows the Vercel timeout.**

## WIDGET (T5)

### Check 4 — `src/components/dashboard/PortedListingsPanel.tsx` — PASS
- **Exists** (~530 lines). `export function PortedListingsPanel()` `:133`.
- `useQuery` x3 (live): `:134` `api.ported_listings.diff`, `:135` `api.ported_listings.list`, `:138` `api.ported_listings.getConfig {key:"leo"}`.
- Side-by-side original vs ported: row rendering with `gradientCss` swatch preview (`:159-171` `linear-gradient(...)`), per-image `<a download={\`${sanitizeFilename(row.name)}.png\`}>` (`:525`).
- Bulk ZIP: `downloadAllZip()` `:232`, dynamic `const JSZip = (await import("jszip")).default` `:237`, `new JSZip()` `:238`, `a.download="ported-listings.zip"` `:261`. `jszip ^3.10.1` present in `package.json:46` (+ `@types/jszip` `:40`).
- Buttons: "Detect Leo gradient" / "Re-detect" `:356,371`, "Run port (N)" `:392` gated on `hasGradient` `:384`, ZIP download `:397`.
- Glassmorphism: imports `Card` from `@/components/ui/Card` `:5` + `SkeletonBlock` `:6` (same shared primitives as sibling panels); inline status pills via rgba backgrounds (`:105,115,125`); progress bars `rounded-full` consistent with CompetitorIntelPanel (`:120,124`).

### Check 5 — REGISTERED — PASS
- `src/lib/dashboard/widget-registry.ts:29` `import { PortedListingsPanel } from "@/components/dashboard/PortedListingsPanel";`
- `:74` (inside `PANEL_WIDGETS` array declared `:42`): `{ id: "ported-listings", label: "Ported Listings", component: PortedListingsPanel },`
- Flows into `DEFAULT_PANEL_ORDER` (`:128`).

### Check 6 — No missing deps — PASS
- `lucide-react`: ABSENT from the widget (grep of widget body found no `lucide` line) AND absent from `package.json` (no lucide entry). Icons are inline `<svg>` with comment "(lucide is not installed); existing panels use plain glyphs/SVG." (`:36`).
- All other imports resolve: `convex/react` (useQuery), `react` (useMemo/useState), `convex/_generated/api`, `@/components/ui/Card`, `@/components/ui/SkeletonBlock`, dynamic `jszip`.

## SUMMARY
- Checks 1–6: PASS.
- No route would blow the Vercel timeout (async kick-off + VPS self-orchestration + Convex callback).
- No missing dependency. Widget is registered.
- ONE design note on 1(c): R2 PNG upload is performed by the VPS, not the serverless route. The route delegates the heavy batch + R2 writes to the VPS and only seeds/returns. This is the correct timeout-safe design; r2-client `getS3Client`/`getR2PublicBase` exist but are used VPS-side, not in the route.
