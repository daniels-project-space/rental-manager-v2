# Convex `ported_listings` Reverification — Phase B (read-only)

Date: 2026-06-18
Deployment (from `.env.local`, unchanged):
- `CONVEX_DEPLOYMENT=dev:hearty-oyster-600` (team Daniels-Project-Space, project rental-manager-v2)
- `NEXT_PUBLIC_CONVEX_URL=https://hearty-oyster-600.convex.cloud`

NOTE: the `dev:` label is cosmetic — hearty-oyster-600 is the LIVE RMv2 prod deployment.

---

## Check 1 — Schema: `ported_listings` table + config doc  →  PASS

`convex/schema.ts`:
```
1614:  ported_listings: defineTable({
1615-    productId: v.string(),
1616-    accountSlug: v.string(),
1617-    name: v.string(),
1618-    dbImageUrl: v.string(),
1619-    masterItemId: v.optional(v.string()),
1620-    status: v.union(v.literal("pending"), v.literal("ported"), ...),
       ...
1635:  ported_listings_config: defineTable({
1636-    key: v.string(),
1637-    gradientProfile: v.any(),
1638-    swatches: v.array(v.string()),
1639-    orientation: v.optional(v.string()),
1640-    leoSampleCount: v.optional(v.number()),
1641-    detectedAt: v.optional(v.number()),
1642-  }).index("by_key", ["key"]),
```
Both the `ported_listings` table (line 1614) and the gradient-profile config table `ported_listings_config` (line 1635, holds `gradientProfile`/`swatches`/`orientation`) exist.

## Check 2 — `convex/ported_listings.ts` exports  →  PASS

File exists (6985 bytes). Exported symbols:
```
54:  export const diff      = query({   // DB Cinema products with no Leo match
108: export const list      = query({   // all ported_listings rows
117: export const upsert    = mutation({// upsert a ported_listing row
170: export const getConfig = query({   // gradient config getter
182: export const setConfig = mutation({// gradient config setter
```
All four required kinds present: diff query, list query, upsert mutation, config getter + setter.

## Check 3 — Deployed to hearty-oyster-600  →  PASS

`npx convex function-spec` (against repo's existing CONVEX_DEPLOYMENT):
```
ported_listings.js:diff
ported_listings.js:list
ported_listings.js:upsert
ported_listings.js:getConfig
ported_listings.js:setConfig
```
Plus `npx convex run ported_listings:diff '{}'` executed successfully (read-only), returning live data — double proof of deployment.

## Check 4 — Diff numbers  →  PASS (matches prior run exactly)

`npx convex run ported_listings:diff '{}'`:
```json
{ "counts": { "dbTotal": 405, "leoTotal": 110, "missingCount": 262 } }
```
- DB Cinema (dbcinema) products: **405**
- Leo products: **110**
- Missing on Leo: **262**

Confirms the prior run (405 / 110 / 262).

## Check 5 — Spot-check "Blackmagic 6K Pro"  →  PASS

In missing set: **true**
```json
{ "productId": "954030",
  "masterItemId": "kn737xq0jcc8a011f6x2m6srt986az25",
  "name": "Blackmagic camera 6k pro BMPCC6K  Bmpcc 6k pro" }
```
Because the diff handler only pushes a product into `missing` when it has neither a masterId match nor a normalized-name match against Leo, its presence proves: no Leo row shares masterItemId `kn737xq0jcc8a011f6x2m6srt986az25` AND no Leo row matches its normalized name.

---

## Matching logic confirmed (`convex/ported_listings.ts:54-105`)

A dbcinema product is MISSING on leo iff:
- `masterItemId` (when present) is NOT in Leo's masterItemId set, AND
- `normalizedName` is NOT in Leo's normalized-name set

```js
const matchedByMaster = masterId !== undefined && toMasterIds.has(masterId);
const matchedByName    = norm !== "" && toNames.has(norm);
if (matchedByMaster || matchedByName) continue;   // matched → not missing
```

`normalizeName` (lines 25-32): lowercase → strip punctuation `[^\p{L}\p{N}\s]` → collapse whitespace → trim. **Exact** set-membership only.

FUZZY FALLBACK: **NONE**. grep for fuzz/leven/similar/distance/includes/startsWith/trigram → no matches. Matching is exact-only (exact masterItemId OR exact normalized name). No partial/fuzzy stage exists.
